{
  "name": "week32",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"npx tsc -b && node build/index.js",
    "produce":"npx tsc -b && node build/producer.js",
    "consume": "tsc -b && node build/consumer.js",
    "produce:user": "npx tsc -b && node build/producer-user.js"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/kafkajs": "^1.9.0"
  }
}
